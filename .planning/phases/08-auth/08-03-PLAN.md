---
phase: 08-auth
plan: 03
type: execute
wave: 2
depends_on: [08-02]
files_modified: [epd7in3e/config.h, epd7in3e/epd7in3e.ino, README.md]
autonomous: false
requirements: [AUTH-06, AUTH-09, AUTH-10]
must_haves:
  truths:
    - "Firmware sends HTTP Basic Auth credentials (admin + APP_PASSWORD constant) on both the /download and /sleep requests"
    - "When APP_PASSWORD is empty in config.h, firmware behavior is unchanged (setAuthorization with empty password is harmless against an open server)"
    - "A human confirms the browser shows the native auth dialog and (optionally) the device fetches an image against a password-protected server"
  artifacts:
    - path: "epd7in3e/config.h"
      provides: "APP_PASSWORD compile-time constant"
      contains: "APP_PASSWORD"
    - path: "epd7in3e/epd7in3e.ino"
      provides: "setAuthorization calls on http and sleepHttp clients"
      contains: "setAuthorization"
  key_links:
    - from: "epd7in3e.ino http client"
      to: "config.h APP_PASSWORD"
      via: "http.setAuthorization(\"admin\", APP_PASSWORD) after http.begin, before http.GET"
      pattern: "setAuthorization\\(\"admin\", APP_PASSWORD\\)"
    - from: "epd7in3e.ino sleepHttp client"
      to: "config.h APP_PASSWORD"
      via: "sleepHttp.setAuthorization(\"admin\", APP_PASSWORD) after sleepHttp.begin, before sleepHttp.GET"
      pattern: "setAuthorization"
---

<objective>
Add HTTP Basic Auth credentials to the ESP32 firmware so the device can authenticate against the now-protected server, then verify the end-to-end flow manually. The firmware uses HTTPClient's native `setAuthorization("admin", APP_PASSWORD)` (no manual base64). Password is a compile-time constant in config.h (matches the existing pattern for SERVER_BASE_URL / HTTP_TIMEOUT).

Purpose: Complete D-05/D-06 — firmware must send credentials so all routes (including the firmware-facing /download and /sleep) work when auth is enabled.
Output: epd7in3e/config.h (APP_PASSWORD constant), epd7in3e/epd7in3e.ino (2 setAuthorization calls), README.md (firmware password note), human verification of browser dialog + device fetch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-auth/08-CONTEXT.md
@.planning/phases/08-auth/08-RESEARCH.md

<interfaces>
<!-- Exact current firmware state the executor will modify. -->

epd7in3e/config.h existing constants (add APP_PASSWORD near SERVER_BASE_URL at line 34):
```cpp
#define SERVER_BASE_URL "http://server.ip:15001"
```

epd7in3e/epd7in3e.ino — image download client `http` (HTTPClient):
- line 67: `http.begin(*secureClient, imageUrl + downloadPath)` (HTTPS branch)
- line 77: `http.begin(*basicClient, imageUrl + downloadPath)` (HTTP branch)
- both branches converge into the retry loop; line ~115 `http.addHeader("batteryCap", ...)`; line 122: `int httpCode = http.GET();`

epd7in3e/epd7in3e.ino — sleep client `sleepHttp` (HTTPClient):
- line 139: `sleepHttp.begin(*sleepSecureClient, sleepUrl)` (HTTPS branch)
- line 144: `sleepHttp.begin(*sleepBasicClient, sleepUrl)` (HTTP branch)
- line 147: `sleepHttp.addHeader("Accept", "application/json");`
- line 148: `int sleepHttpCode = sleepHttp.GET();`

KEY: setAuthorization must be called AFTER begin() and BEFORE GET() (RESEARCH Pitfall 4 — begin() resets client state).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add APP_PASSWORD constant and setAuthorization calls to firmware</name>
  <files>epd7in3e/config.h, epd7in3e/epd7in3e.ino, README.md</files>
  <read_first>
    - epd7in3e/config.h (lines 1-40: constant definitions — add APP_PASSWORD near SERVER_BASE_URL)
    - epd7in3e/epd7in3e.ino (lines 60-125: image download begin/GET region for the `http` client)
    - epd7in3e/epd7in3e.ino (lines 135-155: sleep request begin/GET region for the `sleepHttp` client)
    - .planning/phases/08-auth/08-RESEARCH.md (lines 149-176, 374-391: setAuthorization placement and config.h constant)
  </read_first>
  <action>
1. In `epd7in3e/config.h`, add the APP_PASSWORD constant directly below the `#define SERVER_BASE_URL ...` line (~line 34):
```cpp
// HTTP Basic Auth password (must match server APP_PASSWORD env var). Empty = no auth.
#define APP_PASSWORD ""
```

2. In `epd7in3e/epd7in3e.ino`, add `http.setAuthorization("admin", APP_PASSWORD);` for the image download client. Place it AFTER both begin() branches converge and BEFORE `http.GET()` — the cleanest spot is immediately before the existing `http.addHeader("batteryCap", ...)` call (~line 115), inside the retry block but before the GET. Add:
```cpp
      http.setAuthorization("admin", APP_PASSWORD);
```
This runs once per outer attempt, same scope as the batteryCap header. (begin() already ran above before entering the while loop, so this satisfies after-begin/before-GET.)

IMPORTANT placement check: begin() is called at lines 67/77 BEFORE the `while (retryOnError ...)` loop. setAuthorization must be after begin() and before GET(). Placing it just before `http.addHeader("batteryCap", ...)` (which is before `http.GET()` at line 122) satisfies this. Confirm via read_first that addHeader precedes GET.

3. For the sleep client, add `sleepHttp.setAuthorization("admin", APP_PASSWORD);` AFTER the sleepHttp.begin() branches (lines 139/144) and BEFORE `sleepHttp.GET()` (line 148). Place it immediately before the existing `sleepHttp.addHeader("Accept", "application/json");` line (line 147):
```cpp
            sleepHttp.setAuthorization("admin", APP_PASSWORD);
```
Match the surrounding indentation exactly (the sleep block is more deeply nested).

Do NOT manually base64-encode — setAuthorization handles it. Do NOT call setAuthorization before begin() (state would be wiped — RESEARCH Pitfall 4). Username is the literal string "admin" per D-03.

4. In `README.md`, add one sentence to the Firmware section noting the password must match. Find the firmware setup area (around `### Arduino IDE setup` line ~139 or `### Build and flash`) and add a short note:
```markdown
> **Auth:** If you set `APP_PASSWORD` on the server, set the matching `APP_PASSWORD` constant
> in `epd7in3e/config.h` before flashing so the device can authenticate. Leave it `""` if the
> server has no password.
```
  </action>
  <verify>
    <automated>cd /Users/lennart/Dev/privat/EPF && grep -q '#define APP_PASSWORD' epd7in3e/config.h && grep -c 'setAuthorization("admin", APP_PASSWORD)' epd7in3e/epd7in3e.ino | grep -q 2 && grep -q "APP_PASSWORD" README.md && echo "FIRMWARE_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '#define APP_PASSWORD ""' epd7in3e/config.h`
    - `grep -c 'setAuthorization("admin", APP_PASSWORD)' epd7in3e/epd7in3e.ino` returns 2
    - `grep -n 'http.setAuthorization' epd7in3e/epd7in3e.ino` line number is less than the line of the first `http.GET()` (auth set before GET)
    - `grep -n 'sleepHttp.setAuthorization' epd7in3e/epd7in3e.ino` line number is less than the line of `sleepHttp.GET()`
    - `grep -q "APP_PASSWORD" README.md` (firmware note added)
    - Python test suite unaffected: `python -m pytest tests/ -x` exits 0
  </acceptance_criteria>
  <done>config.h has the APP_PASSWORD constant; both http and sleepHttp clients call setAuthorization("admin", APP_PASSWORD) after begin() and before GET(); README documents the firmware password requirement.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify browser auth dialog and device fetch</name>
  <files>epd7in3e/config.h</files>
  <action>
PAUSE for human verification. No automated work in this task — Plan 02 (server) and Task 1 above (firmware) have already done the implementation. The human confirms the end-to-end auth flow works.

What was built: Server-side HTTP Basic Auth (Plan 02) is live on all 4 routes, and the firmware (Task 1) now sends admin + APP_PASSWORD credentials on its /download and /sleep requests. Auth is opt-in via the APP_PASSWORD env var (server) and the APP_PASSWORD constant (firmware).
  </action>
  <what-built>
    Server-side HTTP Basic Auth (Plan 02) is live on all 4 routes, and the firmware (Task 1 above) now sends admin + APP_PASSWORD credentials on its /download and /sleep requests. Auth is opt-in via the APP_PASSWORD env var (server) and the APP_PASSWORD constant (firmware).
  </what-built>
  <how-to-verify>
    Browser dialog check (AUTH-09):
    1. Start the app locally with a password set:
       `APP_PASSWORD=test python app.py`  (or run the container with `APP_PASSWORD=test`)
    2. Open `http://localhost:5000/` in a browser (or the mapped port, e.g. http://localhost:15151/).
    3. Confirm the browser shows its NATIVE username/password dialog (not a plain 401 error page).
    4. Enter username `admin`, password `test` -> the settings page loads.
    5. Reload, cancel the dialog -> you get "Unauthorized" / 401.
    6. Stop the app, restart WITHOUT APP_PASSWORD -> the page loads with no dialog (opt-in off confirmed).

    Optional firmware end-to-end (AUTH-10 — requires hardware):
    7. Set the same password in `epd7in3e/config.h` (`#define APP_PASSWORD "test"`), flash the XIAO ESP32-S3.
    8. Ensure the server runs with `APP_PASSWORD=test`.
    9. Confirm the device fetches and displays an image (serial log shows HTTP 200, not 401). If no hardware available, note "hardware verification skipped".
  </how-to-verify>
  <verify>Manual — human confirms native browser auth dialog appears, admin/password grants access, and empty-password restart leaves the app open.</verify>
  <done>Human confirms the browser native auth dialog appears and admin credentials work; firmware hardware result recorded or explicitly skipped.</done>
  <resume-signal>Type "approved" if the browser dialog appears and credentials work (and note firmware result or "hardware skipped"), or describe what failed.</resume-signal>
</task>

</tasks>

<verification>
- `grep -c 'setAuthorization("admin", APP_PASSWORD)' epd7in3e/epd7in3e.ino` returns 2.
- Human confirms native browser auth dialog appears and admin/password grants access (AUTH-09).
- Firmware fetch verified on hardware or explicitly skipped (AUTH-10).
- `python -m pytest tests/` still green.
</verification>

<success_criteria>
- AUTH-06: firmware sends Basic Auth on both /download and /sleep via setAuthorization.
- AUTH-09: browser native auth dialog confirmed by human.
- AUTH-10: device authenticates and fetches image (or hardware verification explicitly deferred).
</success_criteria>

<output>
After completion, create `.planning/phases/08-auth/08-03-SUMMARY.md`
</output>
