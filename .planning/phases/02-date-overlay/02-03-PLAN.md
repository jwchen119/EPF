---
phase: 02-date-overlay
plan: 03
type: execute
wave: 2
depends_on:
  - 02-02
files_modified:
  - app.py
  - templates/settings.html
autonomous: true
requirements:
  - DO-01
  - DO-03
  - DO-05
must_haves:
  truths:
    - "DEFAULT_CONFIG['immich']['date_overlay_enabled'] is False and ['date_overlay_position'] is 'bottomRight'"
    - "When date_overlay_enabled is True and a valid Immich date is passed to scale_img_in_memory, the rendered BMP contains a date pill in the configured position"
    - "When date_overlay_enabled is False (default), no overlay is drawn regardless of date"
    - "When date_overlay_enabled is True but no date is available, overlay is silently absent — no placeholder text"
    - "The settings.html page renders a Date Overlay card with an enable toggle and a 9-option position select"
    - "POSTing the settings form with new overlay values persists them to config.yaml and updates the globals"
    - "Dead code block in scale_img_in_memory (the commented-out draw_text_with_background and conditional) is removed"
  artifacts:
    - path: "app.py"
      provides: "DEFAULT_CONFIG keys, globals, update_app_config wiring, scale_img_in_memory signature change, settings POST handler, serve_immich_image date pass-through"
      contains: "date_overlay_enabled, date_overlay_position, immich_date_raw"
    - path: "templates/settings.html"
      provides: "Date Overlay card UI with toggle and 9-position select"
      contains: "date_overlay_enabled, date_overlay_position, Date Overlay"
  key_links:
    - from: "app.scale_img_in_memory"
      to: "app.parse_photo_date and app.draw_date_overlay"
      via: "direct call inside scale_img_in_memory after Image.fromarray"
      pattern: "draw_date_overlay\\(output_img"
    - from: "app.serve_immich_image"
      to: "app.scale_img_in_memory"
      via: "scale_img_in_memory(image, immich_date_raw=selected_image['exifInfo']['dateTimeOriginal'])"
      pattern: "immich_date_raw="
    - from: "templates/settings.html"
      to: "app.settings POST handler"
      via: "form fields date_overlay_enabled and date_overlay_position"
      pattern: "name=\"date_overlay_(enabled|position)\""
---

<objective>
Wire Plan 02's helpers into the request pipeline and expose them through config and the settings UI. After this plan, all 9 Wave 0 tests pass and the overlay can be controlled end-to-end: config.yaml <-> settings UI <-> rendered image.

Purpose: Close DO-03 and DO-05 contracts, finish DO-01 Immich path, remove the dead code block in `scale_img_in_memory`.
Output: Functioning date overlay, off by default, configurable via UI, 9/9 tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-date-overlay/02-CONTEXT.md
@.planning/phases/02-date-overlay/02-RESEARCH.md
@.planning/phases/02-date-overlay/02-02-PLAN.md
@app.py
@templates/settings.html

<interfaces>
Functions already created by Plan 02 (DO NOT redefine, just call):
```python
def parse_photo_date(raw_str) -> str | None: ...
def draw_date_overlay(output_img, text, font, position_str, padding=6) -> None: ...
POSITIONS  # 9-key dict
```

New module-level globals this plan adds:
```python
date_overlay_enabled: bool   # default False
date_overlay_position: str   # default 'bottomRight'
```

New `scale_img_in_memory` signature:
```python
def scale_img_in_memory(image, target_width=1200, target_height=1600,
                        bg_color=(255, 255, 255), immich_date_raw=None):
```

Reference UI pattern: settings.html lines ~391-427 ("Display Settings" card with select fields).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add config keys, globals, and update_app_config() wiring</name>
  <files>app.py</files>
  <read_first>
    - /Users/lennart/Dev/privat/EPF/app.py (lines 30-46 DEFAULT_CONFIG; lines 48-62 global decls; lines 473-508 update_app_config)
    - /Users/lennart/Dev/privat/EPF/.planning/phases/02-date-overlay/02-RESEARCH.md (lines 167-190 config integration pattern; Pitfall 1 at lines 269-278)
    - /Users/lennart/Dev/privat/EPF/tests/test_date_overlay.py (test_default_config defines exact contract)
  </read_first>
  <behavior>
    GREEN target: test_default_config
    - DEFAULT_CONFIG['immich']['date_overlay_enabled'] is exactly False (Python bool, not None)
    - DEFAULT_CONFIG['immich']['date_overlay_position'] is exactly 'bottomRight'
    Module-level globals date_overlay_enabled and date_overlay_position exist and update via update_app_config().
  </behavior>
  <action>
1. In `app.py`, modify the `DEFAULT_CONFIG['immich']` dict (lines 30-46). After the existing `'wakeup_interval': 60,` line, append two new keys:

```
        'wakeup_interval': 60,          # Default 60 minutes (1 hour)
        'date_overlay_enabled': False,  # D-01: overlay off by default
        'date_overlay_position': 'bottomRight',  # D-05: default position
```

2. After the existing global initialization block (after line 62, the `sleep_end_minute = ...` line), append two new module-level globals BEFORE any function definition:

```
date_overlay_enabled = DEFAULT_CONFIG['immich']['date_overlay_enabled']
date_overlay_position = DEFAULT_CONFIG['immich']['date_overlay_position']
```

3. In `update_app_config()` (function starts at line 473), update the `global` statement on line 475 by appending `, date_overlay_enabled, date_overlay_position` at the end. The new line reads:

```
    global current_config, url, albumname, rotationAngle, img_enhanced, img_contrast, strength, display_mode, image_order, sleep_start_hour, sleep_end_hour, sleep_start_minute, sleep_end_minute, date_overlay_enabled, date_overlay_position
```

4. In the same `update_app_config()` function body, after the `sleep_end_minute = new_config['immich']['sleep_end_minute']` line (around line 506), append:

```
    date_overlay_enabled = new_config['immich'].get('date_overlay_enabled', False)
    date_overlay_position = new_config['immich'].get('date_overlay_position', 'bottomRight')
```

Use `.get(..., default)` not `[key]` indexing — old config.yaml files lack these keys (per Pitfall 1).
  </action>
  <verify>
    <automated>cd /Users/lennart/Dev/privat/EPF && pytest tests/test_date_overlay.py::test_default_config -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "'date_overlay_enabled': False" /Users/lennart/Dev/privat/EPF/app.py` returns at least 1 line inside DEFAULT_CONFIG
    - `grep -n "'date_overlay_position': 'bottomRight'" /Users/lennart/Dev/privat/EPF/app.py` returns at least 1 line inside DEFAULT_CONFIG
    - `grep -n "^date_overlay_enabled = " /Users/lennart/Dev/privat/EPF/app.py` returns >= 1 line (module-level initial assignment)
    - `grep -n "date_overlay_enabled, date_overlay_position" /Users/lennart/Dev/privat/EPF/app.py` returns at least 1 line (the global statement)
    - `grep -n "new_config\['immich'\]\.get('date_overlay_enabled'" /Users/lennart/Dev/privat/EPF/app.py` returns 1 line
    - `pytest tests/test_date_overlay.py::test_default_config -x` exits 0
    - `python -m py_compile /Users/lennart/Dev/privat/EPF/app.py` exits 0
  </acceptance_criteria>
  <done>DEFAULT_CONFIG has both keys with correct types/defaults. update_app_config reads them with .get fallback. test_default_config passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire overlay into scale_img_in_memory, remove dead code, pass Immich date through</name>
  <files>app.py</files>
  <read_first>
    - /Users/lennart/Dev/privat/EPF/app.py (lines 180-365 = full scale_img_in_memory function including dead block; lines 722-801 = serve_immich_image; line 792 in particular = call site)
    - /Users/lennart/Dev/privat/EPF/.planning/phases/02-date-overlay/02-RESEARCH.md (lines 84-98 = pipeline insertion point; lines 192-211 = passing Immich date; lines 349-367 = exact insertion snippet; Pitfall 3 at lines 290-298 = MUST delete dead code)
    - /Users/lennart/Dev/privat/EPF/tests/test_date_overlay.py (test_overlay_disabled and test_overlay_no_date define exact contract — both monkeypatch the module globals and call scale_img_in_memory(image, immich_date_raw=...))
  </read_first>
  <behavior>
    GREEN targets: test_overlay_disabled, test_overlay_no_date
    - scale_img_in_memory new signature accepts `immich_date_raw=None` kwarg
    - When `date_overlay_enabled` global is False, no overlay is drawn (image has zero black pixels for a pure white input)
    - When `date_overlay_enabled` is True but `parse_photo_date(immich_date_raw) or parse_photo_date(date_time_raw)` returns None, no overlay is drawn
    - When both enabled AND a date is available, overlay is drawn via `draw_date_overlay` at position `date_overlay_position`
    - serve_immich_image passes `immich_date_raw=selected_image.get('exifInfo', {}).get('dateTimeOriginal')` to scale_img_in_memory
    - The OLD nested `def draw_text_with_background` function and its commented-out call (line 358) are DELETED entirely
  </behavior>
  <action>
1. Change the signature of `scale_img_in_memory` at line 180 from:
```
def scale_img_in_memory(image, target_width=1200, target_height=1600, bg_color=(255, 255, 255)):
```
to:
```
def scale_img_in_memory(image, target_width=1200, target_height=1600, bg_color=(255, 255, 255), immich_date_raw=None):
```

2. Locate the dead code block inside `scale_img_in_memory` spanning approximately lines 255-358. This block includes:
   - EXIF extraction (`exif = image._getexif()` / tag 36867 / `date_time_raw`)
   - The nested `def draw_text_with_background(...)` definition (line 274)
   - The commented-out call `# draw_text_with_background(draw, formatted_time, font)` near line 358

   DELETE the nested `draw_text_with_background` function definition entirely. KEEP the EXIF extraction that produces `date_time_raw` (the local-image fallback for DO-01) — but move/restructure it so it runs only once and produces a single variable `date_time_raw` that may be None. If the existing EXIF extraction logic is wrapped inside the dead block, lift the minimal version out:

```python
    # Extract EXIF date from local image as fallback (DO-01 local path)
    date_time_raw = None
    try:
        exif = image._getexif()
        if exif:
            date_time_raw = exif.get(36867) or exif.get(306)
    except (AttributeError, Exception):
        date_time_raw = None
```

   Place this BEFORE the `load_scaled(...)` call so it runs on the original image with intact EXIF.

3. Locate the line `output_img = Image.fromarray(output_img, mode="RGB")` (search for this exact string). IMMEDIATELY AFTER that line, insert the overlay block:

```python
    # Date overlay (DO-01 + DO-02 + DO-03 + DO-04). Off by default (D-01); silently hidden when no date (D-03).
    if date_overlay_enabled:
        date_str = parse_photo_date(immich_date_raw) or parse_photo_date(date_time_raw)
        if date_str:
            try:
                font = ImageFont.truetype(
                    "/usr/share/fonts/truetype/dejavu/DejaVuSans-Bold.ttf", 26)
            except (IOError, OSError):
                font = ImageFont.load_default()
            draw_date_overlay(output_img, date_str, font, date_overlay_position, padding=6)
```

4. Ensure the very next line is `img_io = io.BytesIO()` followed by `output_img.save(img_io, 'BMP')` (existing code — do not duplicate).

5. In `serve_immich_image()` at approximately line 792, change:
```
    processed_image = scale_img_in_memory(image)
```
to:
```
    immich_date_raw = selected_image.get('exifInfo', {}).get('dateTimeOriginal')
    processed_image = scale_img_in_memory(image, immich_date_raw=immich_date_raw)
```

6. Do NOT change `serve_local_image()` — the EXIF fallback path inside `scale_img_in_memory` already handles it after the lift in step 2.

7. Verify no dangling references to the deleted `draw_text_with_background` remain: `grep -n "draw_text_with_background" /Users/lennart/Dev/privat/EPF/app.py` MUST return 0 lines after this task.
  </action>
  <verify>
    <automated>cd /Users/lennart/Dev/privat/EPF && pytest tests/test_date_overlay.py::test_overlay_disabled tests/test_date_overlay.py::test_overlay_no_date -x -q 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def scale_img_in_memory(image, target_width=1200, target_height=1600, bg_color=(255, 255, 255), immich_date_raw=None)" /Users/lennart/Dev/privat/EPF/app.py` returns exactly 1 line
    - `grep -c "draw_text_with_background" /Users/lennart/Dev/privat/EPF/app.py` returns exactly 0
    - `grep -n "if date_overlay_enabled:" /Users/lennart/Dev/privat/EPF/app.py` returns at least 1 line
    - `grep -n "draw_date_overlay(output_img" /Users/lennart/Dev/privat/EPF/app.py` returns exactly 1 line
    - `grep -n "immich_date_raw = selected_image.get('exifInfo'" /Users/lennart/Dev/privat/EPF/app.py` returns 1 line
    - `grep -n "scale_img_in_memory(image, immich_date_raw=immich_date_raw)" /Users/lennart/Dev/privat/EPF/app.py` returns 1 line
    - `python -m py_compile /Users/lennart/Dev/privat/EPF/app.py` exits 0
    - `pytest tests/test_date_overlay.py::test_overlay_disabled -x` exits 0
    - `pytest tests/test_date_overlay.py::test_overlay_no_date -x` exits 0
    - `pytest tests/test_date_overlay.py -q` reports 9 passed, 0 failed
  </acceptance_criteria>
  <done>scale_img_in_memory accepts immich_date_raw, dead code is gone, overlay is wired behind the enabled flag with silent-hidden fallback. serve_immich_image passes Immich date. All 9 Wave 0 tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Add Date Overlay card to settings UI and wire settings() POST handler</name>
  <files>templates/settings.html, app.py</files>
  <read_first>
    - /Users/lennart/Dev/privat/EPF/templates/settings.html (lines 391-427 = the "Display Settings" card with select fields — mirror this pattern exactly)
    - /Users/lennart/Dev/privat/EPF/app.py (lines 569-631 = settings() route, especially the new_config dict construction at lines 589-605)
    - /Users/lennart/Dev/privat/EPF/.planning/phases/02-date-overlay/02-RESEARCH.md (lines 215-245 = exact HTML pattern; lines 184-190 = POST handler pattern; Pitfall 2 at lines 282-288 = use select not checkbox)
  </read_first>
  <action>
1. Edit `templates/settings.html`. Find the closing `</div>` of the "Image Enhancement" or "Sleep" card (locate by inspecting the existing structure — there is a series of `<div class="card">...</div>` blocks). Insert a new card block at a sensible location (after the "Image Enhancement" card and before "Sleep" / submit button). Use EXACTLY this HTML:

```html
            <div class="card">
                <h2 class="card-title">Date Overlay</h2>
                <div class="form-group">
                    <label for="date_overlay_enabled">Show Photo Date:</label>
                    <select id="date_overlay_enabled" name="date_overlay_enabled">
                        <option value="off" {% if not config['immich'].get('date_overlay_enabled', false) %}selected{% endif %}>Off</option>
                        <option value="on"  {% if config['immich'].get('date_overlay_enabled', false) %}selected{% endif %}>On</option>
                    </select>
                    <div class="small-text">Display the date the photo was taken on the image. Off by default.</div>
                </div>
                <div class="form-group">
                    <label for="date_overlay_position">Overlay Position:</label>
                    <select id="date_overlay_position" name="date_overlay_position">
                        <option value="topLeft"      {% if config['immich'].get('date_overlay_position', 'bottomRight') == 'topLeft' %}selected{% endif %}>Top Left</option>
                        <option value="topCenter"    {% if config['immich'].get('date_overlay_position', 'bottomRight') == 'topCenter' %}selected{% endif %}>Top Center</option>
                        <option value="topRight"     {% if config['immich'].get('date_overlay_position', 'bottomRight') == 'topRight' %}selected{% endif %}>Top Right</option>
                        <option value="centerLeft"   {% if config['immich'].get('date_overlay_position', 'bottomRight') == 'centerLeft' %}selected{% endif %}>Center Left</option>
                        <option value="center"       {% if config['immich'].get('date_overlay_position', 'bottomRight') == 'center' %}selected{% endif %}>Center</option>
                        <option value="centerRight"  {% if config['immich'].get('date_overlay_position', 'bottomRight') == 'centerRight' %}selected{% endif %}>Center Right</option>
                        <option value="bottomLeft"   {% if config['immich'].get('date_overlay_position', 'bottomRight') == 'bottomLeft' %}selected{% endif %}>Bottom Left</option>
                        <option value="bottomCenter" {% if config['immich'].get('date_overlay_position', 'bottomRight') == 'bottomCenter' %}selected{% endif %}>Bottom Center</option>
                        <option value="bottomRight"  {% if config['immich'].get('date_overlay_position', 'bottomRight') == 'bottomRight' %}selected{% endif %}>Bottom Right</option>
                    </select>
                </div>
            </div>
```

Match indentation of surrounding cards (typically 12 spaces inside `<form>`).

2. In `app.py` `settings()` POST handler at lines 589-605, inside the `new_config['immich']` dict literal, after the `'wakeup_interval': ...` entry, append two new entries:

```python
                'wakeup_interval': int(request.form.get('wakeup_interval', current_config['immich']['wakeup_interval'])),
                'date_overlay_enabled': request.form.get('date_overlay_enabled', 'off') == 'on',
                'date_overlay_position': request.form.get('date_overlay_position', 'bottomRight'),
```

The explicit default `'off'` to `.get` guarantees a real bool even on malformed submissions (Pitfall 2).
  </action>
  <verify>
    <automated>cd /Users/lennart/Dev/privat/EPF && python -m py_compile app.py && grep -c "Date Overlay" templates/settings.html && grep -c "date_overlay_enabled\|date_overlay_position" templates/settings.html && grep -c "request.form.get('date_overlay_enabled', 'off') == 'on'" app.py && pytest tests/test_date_overlay.py -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Date Overlay" /Users/lennart/Dev/privat/EPF/templates/settings.html` returns >= 1
    - `grep -c 'name="date_overlay_enabled"' /Users/lennart/Dev/privat/EPF/templates/settings.html` returns exactly 1
    - `grep -c 'name="date_overlay_position"' /Users/lennart/Dev/privat/EPF/templates/settings.html` returns exactly 1
    - `grep -cE 'value="(topLeft|topCenter|topRight|centerLeft|center|centerRight|bottomLeft|bottomCenter|bottomRight)"' /Users/lennart/Dev/privat/EPF/templates/settings.html` returns >= 9 (all 9 positions present in the select)
    - `grep -n "request.form.get('date_overlay_enabled', 'off') == 'on'" /Users/lennart/Dev/privat/EPF/app.py` returns 1 line
    - `grep -n "request.form.get('date_overlay_position', 'bottomRight')" /Users/lennart/Dev/privat/EPF/app.py` returns 1 line
    - `python -m py_compile /Users/lennart/Dev/privat/EPF/app.py` exits 0
    - `pytest tests/test_date_overlay.py -q` reports 9 passed, 0 failed
    - Optional smoke: starting the Flask app and visiting `/settings` shows the new card (manual verification — not blocking)
  </acceptance_criteria>
  <done>Settings UI exposes both overlay controls, POST handler persists them, all 9 Wave 0 tests still green. Manual user can now toggle and pick position from the browser.</done>
</task>

</tasks>

<verification>
- All 9 tests pass: `pytest tests/test_date_overlay.py -v` reports 9 passed
- No dead overlay code remains: `grep -c "draw_text_with_background" app.py` returns 0
- App still imports cleanly: `python -c "import app; print(app.date_overlay_enabled, app.date_overlay_position)"` prints `False bottomRight`
- Settings HTML is syntactically valid Jinja2 (no `{% if` without matching `{% endif %}` errors when rendered — server boots without template error)
</verification>

<success_criteria>
- 9/9 Wave 0 tests GREEN
- Overlay can be enabled/disabled and positioned via settings UI; config.yaml round-trips correctly
- Existing deployments unaffected (overlay off by default per D-01)
- Manual verifications from VALIDATION.md remain (visual on-device check, UI persistence across reload)
</success_criteria>

<output>
After completion, create `.planning/phases/02-date-overlay/02-03-SUMMARY.md`
</output>
