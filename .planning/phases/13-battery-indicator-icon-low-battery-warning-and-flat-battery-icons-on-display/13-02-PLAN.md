---
phase: 13-battery-indicator-icon-low-battery-warning-and-flat-battery-icons-on-display
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - app.py
  - templates/settings.html
autonomous: false
requirements: [BATIND-04, BATIND-05]

must_haves:
  truths:
    - "User can enable/disable the battery indicator and pick its position in the settings UI"
    - "The chosen enable/position persist across config reload"
    - "A low/flat battery shows the icon on the rendered image at the configured position"
    - "A healthy battery, a disabled indicator, or a USB/no-data device shows no icon"
    - "Old config.yaml files without the two new keys still load and render"
  artifacts:
    - path: "app.py"
      provides: "two config keys + globals + update_app_config/.get reads + POST reads + call site in scale_img_in_memory"
      contains: "draw_battery_indicator("
    - path: "templates/settings.html"
      provides: "Battery Indicator card with enable select + position select"
      contains: 'name="battery_indicator_position"'
  key_links:
    - from: "scale_img_in_memory()"
      to: "draw_battery_indicator()"
      via: "call after the date overlay block, gated on battery_indicator_enabled == 'on', battery_pct from last_battery_voltage>0"
      pattern: "draw_battery_indicator\\("
    - from: "templates/settings.html selects"
      to: "app.py POST handler + update_app_config"
      via: "battery_indicator_enabled / battery_indicator_position read via request.form.get and .get fallback"
      pattern: "battery_indicator_enabled"
---

<objective>
Wire the `draw_battery_indicator()` function (built in Plan 13-01) into the application: add the
two persisted config keys, load them into module globals, read them in `update_app_config()` and
the settings POST handler with `.get()` fallback, call `draw_battery_indicator()` from
`scale_img_in_memory()` right after the date overlay block (gated on the enable flag and on real
battery data), and add a "Battery Indicator" card to the settings UI with an Enable select and a
Position select. Finish with a human-verify checkpoint.

Purpose: Make the low-battery warning icon configurable and active in the live render pipeline.
Output: 2 new config keys end-to-end (DEFAULT_CONFIG -> globals -> update_app_config -> POST ->
call site) and a new settings card; human-confirmed render + persistence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-battery-indicator-icon-low-battery-warning-and-flat-battery-icons-on-display/13-CONTEXT.md

<interfaces>
Key contracts the executor needs. Extracted from the codebase — use directly, no exploration needed.

app.py:42-67 — DEFAULT_CONFIG['immich'] dict. Last two existing keys are overlay_margin_h /
overlay_margin_v (lines 65-66). Add the two new keys at the end of this dict (before the closing
brace at line 67).

app.py:325-337 — module-level globals initialized from DEFAULT_CONFIG (the block that ends with
overlay_margin_h / overlay_margin_v at lines 336-337). Add two new module-level globals here.

app.py:386-387 — battery state globals:
    last_battery_voltage = 0
    last_battery_update = 0

app.py:866-886 — calculate_battery_percentage(voltage) -> float 0..100 (already exists).

app.py:752-778 — update_app_config() global declaration block (ends with overlay_margin_h,
overlay_margin_v at lines 777-778). app.py:809-821 — the `.get()` read block (margin reads at
820-821). Add the two new keys in BOTH the global statement and the read block.

app.py:909-965 — settings POST handler new_config build. Existing select-toggle pattern
(date_overlay_enabled at line 934):
    'date_overlay_enabled': request.form.get('date_overlay_enabled', 'off') == 'on',
NOTE: battery_indicator_enabled stores the STRING 'on'/'off' (NOT a bool) per D-15 — it mirrors
the select pattern but the value is compared as a string at the call site. The margin int reads
end at lines 959-964. Add the two new reads before the closing braces at 965-966.

app.py:575-611 — the date overlay block inside scale_img_in_memory(). It ends at line 611 (the
closing paren of the draw_date_overlay(...) call); line 612 is blank, then line 613 starts
"# Save image into ram". The `rotation` and `overlay_font_size` variables are already in scope
in this function (rotation is the local used at line 603; overlay_font_size at line 593). Insert
the battery indicator block AFTER line 611 and BEFORE the "# Save image into ram" comment.

app.py:894-902 — the settings() route reads battery like this; the call site uses the simpler
global-direct form (no 1-hour freshness window):
    battery_pct = calculate_battery_percentage(last_battery_voltage) if last_battery_voltage > 0 else 0
The > 0 guard prevents a false flat icon on USB/no-data devices (D-07).

templates/settings.html:579-595 — the Date Overlay card ends with the Vertical Margin slider and
its closing </div> at line 595. Line 597 opens the next card: <div class="card"><h2 class=
"card-title">Power Management</h2>. Insert the NEW Battery Indicator card BETWEEN line 595 and 597.

templates/settings.html:490-494 — the established select on/off toggle pattern to copy:
    <select id="geo_overlay_enabled" name="geo_overlay_enabled">
        <option value="off" {% if not config['immich'].get('geo_overlay_enabled', true) %}selected{% endif %}>Off</option>
        <option value="on"  {% if config['immich'].get('geo_overlay_enabled', true) %}selected{% endif %}>On</option>
    </select>

templates/settings.html:506-517 — the 9-option position dropdown to copy verbatim (swap the name
and the .get default to battery_indicator_position / 'topRight').
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the two config keys through DEFAULT_CONFIG, globals, update_app_config, and POST handler</name>
  <files>app.py</files>
  <read_first>
    - app.py (lines 42-67 — DEFAULT_CONFIG['immich'])
    - app.py (lines 325-337 — module global init block)
    - app.py (lines 750-821 — update_app_config global statement + .get reads)
    - app.py (lines 909-966 — POST handler new_config build)
    - .planning/phases/13-battery-indicator-icon-low-battery-warning-and-flat-battery-icons-on-display/13-CONTEXT.md (D-15, D-16, D-17)
  </read_first>
  <action>
    Make four edits in app.py, exactly mirroring how overlay_margin_h/overlay_margin_v are wired.

    1. DEFAULT_CONFIG['immich'] (insert after the overlay_margin_v line, app.py:66, before the
       closing brace at line 67) — BATIND-04 / D-16:

           'battery_indicator_enabled': 'on',        # D-15/D-16: show low-battery icon; select on/off
           'battery_indicator_position': 'topRight',  # D-08/D-09/D-16: POSITIONS key, default topRight

    2. Module global init block (after app.py:337 `overlay_margin_v = DEFAULT_CONFIG['immich']['overlay_margin_v']`):

           battery_indicator_enabled = DEFAULT_CONFIG['immich']['battery_indicator_enabled']
           battery_indicator_position = DEFAULT_CONFIG['immich']['battery_indicator_position']

    3a. update_app_config() global statement (app.py:752-778) — append the two names to the
        backslash-continued global declaration after `overlay_margin_v` (line 778). Change the
        final line so it continues:

           ... \
           overlay_margin_h, \
           overlay_margin_v, \
           battery_indicator_enabled, \
           battery_indicator_position

    3b. update_app_config() read block (after app.py:821 overlay_margin_v read) — D-17 `.get()`
        fallback. battery_indicator_enabled is a STRING (no int/bool cast — it is compared to 'on'
        at the call site):

           battery_indicator_enabled = new_config['immich'].get('battery_indicator_enabled', 'on')
           battery_indicator_position = new_config['immich'].get('battery_indicator_position', 'topRight')

    4. POST handler new_config build (after the overlay_margin_v read at app.py:962-964, before the
       closing braces at 965-966) — D-15/D-17. Mirror the select-string pattern but KEEP the raw
       string (do NOT convert to bool), defaulting to the current value then 'on':

           'battery_indicator_enabled': request.form.get(
               'battery_indicator_enabled', current_config['immich'].get('battery_indicator_enabled', 'on')
           ),
           'battery_indicator_position': request.form.get(
               'battery_indicator_position', current_config['immich'].get('battery_indicator_position', 'topRight')
           ),

    Do NOT add the call to draw_battery_indicator() in this task — that is Task 2. Do NOT touch the
    settings.html template here — that is Task 3.
  </action>
  <verify>
    <automated>python -c "import app; assert app.DEFAULT_CONFIG['immich']['battery_indicator_enabled']=='on' and app.DEFAULT_CONFIG['immich']['battery_indicator_position']=='topRight'; assert app.battery_indicator_enabled=='on' and app.battery_indicator_position=='topRight'; app.update_app_config({'immich': dict(app.DEFAULT_CONFIG['immich'])}); print('config round-trip OK')" && python -c "import app; cfg={'immich': {k:v for k,v in app.DEFAULT_CONFIG['immich'].items() if not k.startswith('battery_indicator')}}; app.update_app_config(cfg); assert app.battery_indicator_enabled=='on' and app.battery_indicator_position=='topRight'; print('backward-compat .get fallback OK')"</automated>
  </verify>
  <acceptance_criteria>
    - grep "'battery_indicator_enabled': 'on'," app.py returns 1 match
    - grep "'battery_indicator_position': 'topRight'," app.py returns 1 match
    - grep "battery_indicator_enabled = new_config\['immich'\].get('battery_indicator_enabled', 'on')" app.py returns 1 match
    - grep "battery_indicator_position = new_config\['immich'\].get('battery_indicator_position', 'topRight')" app.py returns 1 match
    - python -c "import app; assert app.DEFAULT_CONFIG['immich']['battery_indicator_enabled']=='on'" exits 0
    - python -c "import app; cfg={'immich': {k:v for k,v in app.DEFAULT_CONFIG['immich'].items() if not k.startswith('battery_indicator')}}; app.update_app_config(cfg); assert app.battery_indicator_position=='topRight'" exits 0 (old config without keys still loads)
    - pytest -q exits 0 (no regression)
  </acceptance_criteria>
  <done>battery_indicator_enabled ('on') and battery_indicator_position ('topRight') exist in DEFAULT_CONFIG, are loaded into module globals, are declared+read with .get() fallback in update_app_config(), and are read in the POST handler as raw strings; a config without the keys still loads via fallback and the full suite passes.</done>
</task>

<task type="auto">
  <name>Task 2: Call draw_battery_indicator from scale_img_in_memory after the date overlay</name>
  <files>app.py</files>
  <read_first>
    - app.py (lines 575-617 — date overlay block + the "# Save image into ram" tail in scale_img_in_memory)
    - app.py (lines 386-387 — last_battery_voltage / last_battery_update globals)
    - app.py (lines 866-886 — calculate_battery_percentage)
    - app.py (the scale_img_in_memory function head — confirm whether it has a `global` statement and what it lists)
    - .planning/phases/13-battery-indicator-icon-low-battery-warning-and-flat-battery-icons-on-display/13-CONTEXT.md (D-03, D-05, D-07, D-10, D-11, D-18, D-19)
  </read_first>
  <action>
    In app.py, ensure scale_img_in_memory() can read the new globals. Read access to module-level
    globals does NOT require a `global` statement, so do NOT add one unless the function already
    assigns these names (it does not). Reading last_battery_voltage, battery_indicator_enabled, and
    battery_indicator_position directly is sufficient.

    Insert the battery indicator block immediately AFTER the date overlay block (after the closing
    paren of the draw_date_overlay(...) call at app.py:611) and BEFORE the "# Save image into ram"
    comment at app.py:613. Use the local `rotation` variable already used by the date overlay call,
    and `overlay_font_size` for sizing (D-11, D-18). Color is fixed white per D-03:

        # Low-battery warning icon (BATIND-03/04; warning-only, D-05/D-19).
        # battery_pct comes from the live ADC voltage; 0 (USB/no data) suppresses the icon (D-07).
        if battery_indicator_enabled == 'on':
            battery_pct = (
                calculate_battery_percentage(last_battery_voltage)
                if last_battery_voltage > 0
                else 0
            )
            draw_battery_indicator(
                output_img,
                battery_pct,
                battery_indicator_position,
                rotation,
                overlay_font_size,
                OVERLAY_COLORS.get('white', (255, 255, 255, 255)),
            )

    Notes:
    - draw_battery_indicator itself no-ops when battery_pct > 20, so a healthy battery draws
      nothing even though this block runs.
    - The last_battery_voltage > 0 guard is REQUIRED (D-07): without it, pct=0 on a USB/no-data
      device would be <= 5 and draw a false flat-battery icon.
    - Do NOT gate on date_overlay_enabled — the battery indicator is independent of the date
      overlay (D-10).
  </action>
  <verify>
    <automated>grep -q "draw_battery_indicator(" app.py && grep -q "if battery_indicator_enabled == 'on':" app.py && grep -q "if last_battery_voltage > 0" app.py && python -c "import app; from PIL import Image; img=Image.new('RGB',(1200,1600),(255,255,255)); before=img.tobytes(); app.draw_battery_indicator(img, app.calculate_battery_percentage(3710), 'topRight', 0, 26, (255,255,255,255)); assert img.tobytes()!=before; print('low-battery render OK')" && pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - grep "draw_battery_indicator(" app.py returns at least 2 matches (definition from 13-01 + this call site)
    - grep "if battery_indicator_enabled == 'on':" app.py returns 1 match
    - grep "if last_battery_voltage > 0" app.py returns at least 1 match
    - grep "OVERLAY_COLORS.get('white'" app.py returns 1 match (white icon color, D-03)
    - the draw_battery_indicator( call site appears between the draw_date_overlay block and "# Save image into ram" (verify by reading app.py:611-625)
    - pytest -q exits 0 (no regression)
  </acceptance_criteria>
  <done>scale_img_in_memory() calls draw_battery_indicator() after the date overlay, gated on battery_indicator_enabled == 'on', with battery_pct derived from last_battery_voltage (0 when last_battery_voltage <= 0), the configured position, the current rotation, overlay_font_size, and a white icon color; the suite passes.</done>
</task>

<task type="auto">
  <name>Task 3: Add the Battery Indicator card to settings.html</name>
  <files>templates/settings.html</files>
  <read_first>
    - templates/settings.html (lines 485-595 — Date Overlay card incl. the geo on/off select and the 9-option position dropdown to copy)
    - templates/settings.html (lines 595-600 — the boundary where the new card is inserted, before the Power Management card)
    - .planning/phases/13-battery-indicator-icon-low-battery-warning-and-flat-battery-icons-on-display/13-CONTEXT.md (D-14, D-15)
  </read_first>
  <action>
    In templates/settings.html, insert a NEW card between the Date Overlay card's closing </div>
    (line 595) and the Power Management card's opening <div class="card"> (line 597). It is a
    separate card (D-14) — do NOT add these controls to the Date Overlay or Power Management cards.

    Use the exact select on/off pattern from geo_overlay_enabled (settings.html:490-494) and the
    exact 9-option position dropdown from date_overlay_position (settings.html:506-517), swapping
    names/defaults. The enable default is 'on' and the position default is 'topRight'. Insert
    verbatim:

        <div class="card">
            <h2 class="card-title">Battery Indicator</h2>
            <div class="form-group">
                <label for="battery_indicator_enabled">Show Low-Battery Icon:</label>
                <select id="battery_indicator_enabled" name="battery_indicator_enabled">
                    <option value="off" {% if config['immich'].get('battery_indicator_enabled', 'on') == 'off' %}selected{% endif %}>Off</option>
                    <option value="on"  {% if config['immich'].get('battery_indicator_enabled', 'on') == 'on'  %}selected{% endif %}>On</option>
                </select>
                <div class="small-text">Shows a battery icon only when the device reports a low (≤20%) or flat (≤5%) charge. Never shown on USB power or when no battery data has been received.</div>
            </div>
            <div class="form-group">
                <label for="battery_indicator_position">Icon Position:</label>
                <select id="battery_indicator_position" name="battery_indicator_position">
                    <option value="topLeft"      {% if config['immich'].get('battery_indicator_position', 'topRight') == 'topLeft' %}selected{% endif %}>Top Left</option>
                    <option value="topCenter"    {% if config['immich'].get('battery_indicator_position', 'topRight') == 'topCenter' %}selected{% endif %}>Top Center</option>
                    <option value="topRight"     {% if config['immich'].get('battery_indicator_position', 'topRight') == 'topRight' %}selected{% endif %}>Top Right</option>
                    <option value="centerLeft"   {% if config['immich'].get('battery_indicator_position', 'topRight') == 'centerLeft' %}selected{% endif %}>Center Left</option>
                    <option value="center"       {% if config['immich'].get('battery_indicator_position', 'topRight') == 'center' %}selected{% endif %}>Center</option>
                    <option value="centerRight"  {% if config['immich'].get('battery_indicator_position', 'topRight') == 'centerRight' %}selected{% endif %}>Center Right</option>
                    <option value="bottomLeft"   {% if config['immich'].get('battery_indicator_position', 'topRight') == 'bottomLeft' %}selected{% endif %}>Bottom Left</option>
                    <option value="bottomCenter" {% if config['immich'].get('battery_indicator_position', 'topRight') == 'bottomCenter' %}selected{% endif %}>Bottom Center</option>
                    <option value="bottomRight"  {% if config['immich'].get('battery_indicator_position', 'topRight') == 'bottomRight' %}selected{% endif %}>Bottom Right</option>
                </select>
                <div class="small-text">Independent of the date overlay position — they may share a corner.</div>
            </div>
        </div>

    Do NOT modify app.py here (POST/config reads already added in Task 1).
  </action>
  <verify>
    <automated>grep -c 'name="battery_indicator_position"' templates/settings.html && grep -c 'name="battery_indicator_enabled"' templates/settings.html && python -c "import jinja2; jinja2.Environment(loader=jinja2.FileSystemLoader('templates')).get_template('settings.html'); print('template parses OK')"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'name="battery_indicator_position"' templates/settings.html returns 1
    - grep -c 'name="battery_indicator_enabled"' templates/settings.html returns 1
    - grep -c '<h2 class="card-title">Battery Indicator</h2>' templates/settings.html returns 1
    - grep -c "battery_indicator_position', 'topRight'" templates/settings.html returns 9 (one per position option)
    - grep "battery_indicator_enabled', 'on') == 'on'" templates/settings.html returns 1 match
    - jinja2 template load command exits 0 (no template syntax error)
  </acceptance_criteria>
  <done>A standalone "Battery Indicator" card sits between the Date Overlay and Power Management cards with an Enable on/off select (default on) and a 9-option Position select (default topRight), both using the .get() selected-state pattern; the template still parses without Jinja errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify battery icon renders, positions, and persists (BATIND-05)</name>
  <action>
    No new implementation. Before signalling the human, ensure the full suite is green and the
    template renders: run `pytest -q` and load templates/settings.html via Jinja. Then present the
    verification steps below and wait for approval before marking the plan complete.
  </action>
  <what-built>
    A "Battery Indicator" settings card (Enable on/off + 9-position dropdown), two persisted config
    keys (battery_indicator_enabled, battery_indicator_position), and a call to
    draw_battery_indicator() in the render pipeline that draws a white battery icon only when the
    device reports a low (≤20%) or flat (≤5%) charge — suppressed on USB / no-data and when
    disabled.
  </what-built>
  <how-to-verify>
    1. Start the server locally (e.g. `python app.py`) and open the settings page. Confirm a new
       "Battery Indicator" card appears between the Date Overlay and Power Management cards, with an
       Enable select (default On) and an Icon Position select (default Top Right).
    2. Set Icon Position to a corner (e.g. Bottom Left), Save. Expect: page reloads with the value
       still selected; config.yaml contains battery_indicator_position: bottomLeft and
       battery_indicator_enabled: on.
    3. Simulate a low battery so the icon renders. Easiest: in a Python shell against the running
       module, set `app.last_battery_voltage = 3710` (≈20%) then trigger an image render (visit
       /download or the preview path). Expect: the rendered image shows a battery outline with a
       partial fill at the configured position. Then set `app.last_battery_voltage = 3610` (≈5%)
       and re-render — expect an EMPTY battery outline (flat icon, no fill).
    4. Set `app.last_battery_voltage = 4150` (healthy) and re-render — expect NO battery icon.
    5. Set `app.last_battery_voltage = 0` (USB/no data) and re-render — expect NO battery icon
       (the >0 guard suppresses a false flat warning).
    6. Set the card's Enable to Off, Save, force a low voltage again, re-render — expect NO icon.
    7. (Backward-compat) Confirm an existing config.yaml without the two battery_indicator_* keys
       still loads and renders the card with the defaults (On / Top Right).
  </how-to-verify>
  <resume-signal>Type "approved" if the icon shows partial fill at low, empty outline at flat, nothing when healthy / USB / disabled, persists on save, and old configs still load — or describe what went wrong.</resume-signal>
</task>

</tasks>

<verification>
- python -c "import app; assert app.DEFAULT_CONFIG['immich']['battery_indicator_enabled']=='on' and app.DEFAULT_CONFIG['immich']['battery_indicator_position']=='topRight'" exits 0 (BATIND-04)
- grep "draw_battery_indicator(" app.py returns at least 2 matches (BATIND-04 call site)
- grep -c 'name="battery_indicator_position"' templates/settings.html returns 1 (BATIND-05)
- pytest -q exits 0 (no regression)
- Human confirms render states, persistence, and backward compat (BATIND-05)
</verification>

<success_criteria>
- battery_indicator_enabled ('on') + battery_indicator_position ('topRight') wired end-to-end (DEFAULT_CONFIG, globals, update_app_config, POST), with .get() backward-compat
- scale_img_in_memory() calls draw_battery_indicator() after the date overlay, gated on the enable flag and the last_battery_voltage > 0 guard, with a white icon color
- Settings UI has a standalone Battery Indicator card with enable + position selects
- Full test suite passes and human verification confirms the warning-only behavior end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/13-battery-indicator-icon-low-battery-warning-and-flat-battery-icons-on-display/13-02-SUMMARY.md`
</output>
