---
phase: 06-text-customization-colors-styles-and-border-mode
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - app.py
  - templates/settings.html
autonomous: true
requirements: [TC-08, TC-09]
must_haves:
  truths:
    - "update_app_config() reads all 6 overlay_* keys into module globals via .get() fallback (TC-08 GREEN)"
    - "POST handler parses overlay_font_size and overlay_stroke_width as int and color/style as strings (TC-09 GREEN)"
    - "scale_img_in_memory() loads the font at overlay_font_size and passes resolved colors + style + stroke to draw_date_overlay()"
    - "settings.html Date Overlay card exposes style dropdown, 3 color dropdowns, font-size slider, stroke-width slider — all pre-selected from current config"
    - "A user can change overlay style/colors/font-size/stroke in the UI, save, and the next render reflects it"
  artifacts:
    - path: "app.py"
      provides: "update_app_config global reads, POST handler form parsing, scale_img_in_memory call-site wiring"
      contains: "overlay_font_size = new_config['immich'].get('overlay_font_size'"
    - path: "templates/settings.html"
      provides: "Overlay style/color/font-size/stroke-width controls in Date Overlay card"
      contains: "name=\"overlay_style\""
  key_links:
    - from: "templates/settings.html overlay controls"
      to: "app.py settings() POST handler"
      via: "request.form.get('overlay_*')"
      pattern: "request.form.get\\('overlay_style'"
    - from: "app.py scale_img_in_memory()"
      to: "draw_date_overlay()"
      via: "OVERLAY_COLORS-resolved kwargs + overlay_font_size font load"
      pattern: "style=overlay_style"
    - from: "app.py update_app_config()"
      to: "module globals"
      via: ".get() fallback reads"
      pattern: "overlay_style = new_config\\['immich'\\].get\\('overlay_style'"
---

<objective>
Wire the Phase 6 overlay-customization config end-to-end: read new keys in update_app_config()
(with .get() backward-compat fallback), parse them in the settings() POST handler (int casts for
sliders), pass resolved colors + style + stroke + font size into draw_date_overlay() at the
scale_img_in_memory() call site, and add the UI controls to the Date Overlay card in settings.html.
Turns TC-08 and TC-09 GREEN and completes the phase.

Purpose: Make every overlay setting selectable in the Configuration UI and persisted to config.yaml,
following the established Phase 2 config pattern exactly (D-15 .get() fallback; int casts per pitfall 3/4).
Output: Modified app.py (config wiring) + templates/settings.html (controls).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-text-customization-colors-styles-and-border-mode/06-CONTEXT.md
@.planning/phases/06-text-customization-colors-styles-and-border-mode/06-RESEARCH.md

<interfaces>
<!-- Exact anchors verified 2026-05-29. Plan 02 already added OVERLAY_COLORS, DEFAULT_CONFIG keys, globals, and extended draw_date_overlay(). -->

update_app_config() global statement — app.py:497-512 (ends at `date_overlay_position`).
update_app_config() variable reads — app.py:530-544 (date_overlay_* read via .get() at 543-544).

scale_img_in_memory() overlay call site — app.py:366-374:
```python
if date_overlay_enabled:
    date_str = parse_photo_date(immich_date_raw) or parse_photo_date(date_time_raw)
    if date_str:
        try:
            font = ImageFont.truetype('/usr/share/fonts/truetype/dejavu/DejaVuSans-Bold.ttf', 26)
        except (IOError, OSError):
            font = ImageFont.load_default()
        draw_date_overlay(output_img, date_str, font, date_overlay_position, padding=6, rotation=rotation)
```

settings() POST handler new_config['immich'] dict — app.py:633-658 (date_overlay keys at 656-657).
Pattern for int slider: sleep_start_hour at app.py:643-645 uses int(request.form.get(...)).

settings.html Date Overlay card — templates/settings.html:459-483 (card opens 459, closes 483;
position dropdown 469-482). New controls appended INSIDE this card before its closing </div> (line 483).
Slider pattern reference — templates/settings.html:449-456 (Dithering Strength range input).
updateSliderValue JS — templates/settings.html:595.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire new keys into update_app_config() and scale_img_in_memory() call site</name>
  <files>app.py</files>
  <read_first>
    - app.py:497-512 (update_app_config global statement)
    - app.py:530-544 (update_app_config variable reads — .get() pattern at 543-544)
    - app.py:366-374 (scale_img_in_memory overlay call site)
    - app.py (OVERLAY_COLORS dict added by Plan 02, near line 204)
    - tests/test_overlay_customization.py (TC-08 test_update_config_new_keys — implement to satisfy)
  </read_first>
  <action>
    1. Extend the `global` statement in update_app_config() (app.py:497-512). The statement
       currently ends with `date_overlay_position` (line 512). Append (continuing the
       backslash-continued list):
         date_overlay_position, \
         overlay_style, \
         overlay_bg_color, \
         overlay_text_color, \
         overlay_border_color, \
         overlay_stroke_width, \
         overlay_font_size
       (i.e. add a trailing comma+backslash after date_overlay_position and list the 6 new names).

    2. After the `date_overlay_position = new_config['immich'].get('date_overlay_position', 'bottomRight')`
       line (app.py:544), add these 6 reads using .get() fallback (D-15). Cast the two slider
       values to int (pitfall 3/4):
         overlay_style = new_config['immich'].get('overlay_style', 'background')
         overlay_bg_color = new_config['immich'].get('overlay_bg_color', 'black')
         overlay_text_color = new_config['immich'].get('overlay_text_color', 'white')
         overlay_border_color = new_config['immich'].get('overlay_border_color', 'white')
         overlay_stroke_width = int(new_config['immich'].get('overlay_stroke_width', 2))
         overlay_font_size = int(new_config['immich'].get('overlay_font_size', 26))

    3. Update the overlay call site in scale_img_in_memory() (app.py:366-374). Change the
       hardcoded font size 26 to overlay_font_size and pass the new kwargs (resolve color names
       to RGBA via OVERLAY_COLORS with the D-14 default tuples as fallback):
         try:
             font = ImageFont.truetype(
                 '/usr/share/fonts/truetype/dejavu/DejaVuSans-Bold.ttf',
                 overlay_font_size,
             )
         except (IOError, OSError):
             font = ImageFont.load_default()
         draw_date_overlay(
             output_img, date_str, font, date_overlay_position, padding=6,
             rotation=rotation,
             style=overlay_style,
             bg_color=OVERLAY_COLORS.get(overlay_bg_color, (0, 0, 0, 255)),
             text_color=OVERLAY_COLORS.get(overlay_text_color, (255, 255, 255, 255)),
             border_color=OVERLAY_COLORS.get(overlay_border_color, (255, 255, 255, 255)),
             stroke_width=overlay_stroke_width,
         )
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_overlay_customization.py::test_update_config_new_keys -q && .venv/bin/python -c "import ast; ast.parse(open('app.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "overlay_style = new_config\['immich'\].get('overlay_style', 'background')" app.py` succeeds
    - `grep -q "overlay_stroke_width = int(new_config\['immich'\].get('overlay_stroke_width', 2))" app.py` succeeds
    - `grep -q "overlay_font_size = int(new_config\['immich'\].get('overlay_font_size', 26))" app.py` succeeds
    - `grep -q "style=overlay_style," app.py` succeeds
    - `grep -q "bg_color=OVERLAY_COLORS.get(overlay_bg_color" app.py` succeeds
    - `grep -q "border_color=OVERLAY_COLORS.get(overlay_border_color" app.py` succeeds
    - `grep -q "DejaVuSans-Bold.ttf'," app.py` AND the following line contains `overlay_font_size` (font no longer hardcoded 26 at the overlay call site)
    - `grep -q "overlay_font_size, \\\\$" app.py || grep -q "overlay_font_size$" app.py` (global statement lists it)
    - `.venv/bin/python -m pytest tests/test_overlay_customization.py::test_update_config_new_keys -q` exits 0
    - `.venv/bin/python -c "import ast; ast.parse(open('app.py').read())"` exits 0 (no syntax error)
  </acceptance_criteria>
  <done>update_app_config() reads all 6 keys via .get() (int casts on sliders); scale_img_in_memory() loads font at overlay_font_size and passes resolved colors/style/stroke; TC-08 GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Add POST handler parsing + settings.html overlay controls</name>
  <files>app.py, templates/settings.html</files>
  <read_first>
    - app.py:633-658 (settings() POST new_config dict — date_overlay keys at 656-657, int-cast pattern at 643-645)
    - templates/settings.html:449-483 (Date Overlay card + slider pattern reference)
    - .planning/phases/06-text-customization-colors-styles-and-border-mode/06-RESEARCH.md (lines 204-241: slider + color dropdown HTML patterns)
    - tests/test_overlay_customization.py (TC-09 test_post_handler_font_size_int — implement to satisfy)
  </read_first>
  <action>
    PART A — app.py POST handler (app.py:633-658):
    After the `'date_overlay_position': request.form.get('date_overlay_position', 'bottomRight'),`
    line (app.py:657), add these 6 entries to new_config['immich'] (int casts on sliders per
    pitfall 3/4; color/style are strings):
      'overlay_style': request.form.get('overlay_style', current_config['immich'].get('overlay_style', 'background')),
      'overlay_bg_color': request.form.get('overlay_bg_color', current_config['immich'].get('overlay_bg_color', 'black')),
      'overlay_text_color': request.form.get('overlay_text_color', current_config['immich'].get('overlay_text_color', 'white')),
      'overlay_border_color': request.form.get('overlay_border_color', current_config['immich'].get('overlay_border_color', 'white')),
      'overlay_stroke_width': int(request.form.get('overlay_stroke_width', current_config['immich'].get('overlay_stroke_width', 2))),
      'overlay_font_size': int(request.form.get('overlay_font_size', current_config['immich'].get('overlay_font_size', 26))),

    PART B — templates/settings.html (inside the Date Overlay card, before its closing </div> at
    line 483, after the position dropdown form-group that ends at line 482):

    1. Overlay Style dropdown (D-04/D-05 — select not checkbox):
       <div class="form-group">
           <label for="overlay_style">Overlay Style:</label>
           <select id="overlay_style" name="overlay_style">
               <option value="background" {% if config['immich'].get('overlay_style', 'background') == 'background' %}selected{% endif %}>Background (filled)</option>
               <option value="outline"    {% if config['immich'].get('overlay_style', 'background') == 'outline'    %}selected{% endif %}>Outline (stroke)</option>
           </select>
           <div class="small-text">Background draws a filled rectangle; Outline draws a colored stroke around the text.</div>
       </div>

    2. Three color dropdowns. For each (overlay_bg_color default 'black', overlay_text_color
       default 'white', overlay_border_color default 'white'), emit a form-group with a 6-option
       <select> over black/white/yellow/red/blue/green. Use this exact structure (shown for
       background color; repeat for the other two with the right name/default/label):
       <div class="form-group">
           <label for="overlay_bg_color">Background Color:</label>
           <select id="overlay_bg_color" name="overlay_bg_color">
               <option value="black"  {% if config['immich'].get('overlay_bg_color', 'black') == 'black'  %}selected{% endif %}>Black</option>
               <option value="white"  {% if config['immich'].get('overlay_bg_color', 'black') == 'white'  %}selected{% endif %}>White</option>
               <option value="yellow" {% if config['immich'].get('overlay_bg_color', 'black') == 'yellow' %}selected{% endif %}>Yellow</option>
               <option value="red"    {% if config['immich'].get('overlay_bg_color', 'black') == 'red'    %}selected{% endif %}>Red</option>
               <option value="blue"   {% if config['immich'].get('overlay_bg_color', 'black') == 'blue'   %}selected{% endif %}>Blue</option>
               <option value="green"  {% if config['immich'].get('overlay_bg_color', 'black') == 'green'  %}selected{% endif %}>Green</option>
           </select>
           <div class="small-text">Used in Background style only.</div>
       </div>
       - overlay_text_color: label "Text Color:", name/id="overlay_text_color", default 'white' in
         every .get() and comparison, small-text "Used in both styles."
       - overlay_border_color: label "Border (Stroke) Color:", name/id="overlay_border_color",
         default 'white', small-text "Used in Outline style only."

    3. Font Size slider (D-12/D-13, min 16 max 48, default 26), replicating the Dithering
       Strength slider pattern (settings.html:449-456):
       <div class="form-group">
           <label for="overlay_font_size">Font Size:</label>
           <div class="slider-container">
               <input type="range" id="overlay_font_size" name="overlay_font_size" min="16" max="48" step="1"
                   value="{{ config['immich'].get('overlay_font_size', 26) }}" oninput="updateSliderValue(this)">
               <output class="slider-value">{{ config['immich'].get('overlay_font_size', 26) }}</output>
           </div>
       </div>

    4. Stroke Width slider (D-09/D-10/D-11, min 1 max 5, default 2):
       <div class="form-group">
           <label for="overlay_stroke_width">Stroke Width (Outline only):</label>
           <div class="slider-container">
               <input type="range" id="overlay_stroke_width" name="overlay_stroke_width" min="1" max="5" step="1"
                   value="{{ config['immich'].get('overlay_stroke_width', 2) }}" oninput="updateSliderValue(this)">
               <output class="slider-value">{{ config['immich'].get('overlay_stroke_width', 2) }}</output>
           </div>
       </div>

    No JavaScript show/hide is required (per CONTEXT specifics): always show all controls; the
    small-text labels tell the user which apply per style.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_overlay_customization.py -q && .venv/bin/python -c "import ast; ast.parse(open('app.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "'overlay_style': request.form.get('overlay_style'" app.py` succeeds
    - `grep -q "'overlay_font_size': int(request.form.get('overlay_font_size'" app.py` succeeds
    - `grep -q "'overlay_stroke_width': int(request.form.get('overlay_stroke_width'" app.py` succeeds
    - `grep -q 'name="overlay_style"' templates/settings.html` succeeds
    - `grep -q 'name="overlay_bg_color"' templates/settings.html` succeeds
    - `grep -q 'name="overlay_text_color"' templates/settings.html` succeeds
    - `grep -q 'name="overlay_border_color"' templates/settings.html` succeeds
    - `grep -q 'name="overlay_font_size"' templates/settings.html` succeeds
    - `grep -q 'name="overlay_stroke_width"' templates/settings.html` succeeds
    - `grep -c 'value="green"' templates/settings.html` returns at least 3 (one per color dropdown)
    - `grep -q 'min="16" max="48"' templates/settings.html` succeeds (font size slider)
    - `grep -q 'min="1" max="5"' templates/settings.html` succeeds (stroke width slider)
    - `.venv/bin/python -m pytest tests/test_overlay_customization.py -q` => all 9 pass (TC-01..TC-09 GREEN)
    - `.venv/bin/python -m pytest tests/ -q` => full suite green (Phase 2 + Phase 6, no regression)
    - `.venv/bin/python -c "import ast; ast.parse(open('app.py').read())"` exits 0
  </acceptance_criteria>
  <done>POST handler parses all 6 keys (int casts on sliders); settings.html exposes style dropdown, 3 color dropdowns, and 2 sliders pre-selected from config; TC-09 GREEN and full suite passes.</done>
</task>

</tasks>

<verification>
- `.venv/bin/python -m pytest tests/test_overlay_customization.py -q` => 9 passed (TC-01..TC-09 all GREEN)
- `.venv/bin/python -m pytest tests/ -q` => full suite green (no Phase 2 regression)
- `.venv/bin/python -c "import ast; ast.parse(open('app.py').read())"` => exits 0 (valid syntax)
- Manual (per VALIDATION.md): set style=outline with white text + black border, save, trigger a render, confirm stroke is visible; set text color = bg color, confirm overlay invisible (D-02).
</verification>

<success_criteria>
All 6 overlay settings are selectable in the Configuration UI, persisted to config.yaml, read back
on load via .get() fallback, and applied at render time (font size, style, colors, stroke). TC-08
and TC-09 GREEN; the full Phase 6 contract (TC-01..TC-09) and all Phase 2 tests pass. Existing
deployments upgrading see zero visual change until they configure overlay settings (D-14).
</success_criteria>

<output>
After completion, create `.planning/phases/06-text-customization-colors-styles-and-border-mode/06-03-SUMMARY.md`
</output>
