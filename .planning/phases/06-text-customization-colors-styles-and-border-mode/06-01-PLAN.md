---
phase: 06-text-customization-colors-styles-and-border-mode
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_overlay_customization.py
autonomous: true
requirements: [TC-01, TC-02, TC-03, TC-04, TC-05, TC-06, TC-07]
must_haves:
  truths:
    - "Running pytest produces 7 failing tests in test_overlay_customization.py (RED state)"
    - "Tests lock the OVERLAY_COLORS dict contract (6 palette entries, correct RGBA)"
    - "Tests lock the draw_date_overlay() extended signature contract (style/bg_color/text_color/border_color/stroke_width)"
    - "Existing 13 Phase 2 tests still pass (no regression introduced by the new test file)"
  artifacts:
    - path: "tests/test_overlay_customization.py"
      provides: "Failing TDD contract tests for overlay customization (TC-01..TC-07)"
      min_lines: 90
      contains: "def test_overlay_colors_dict"
  key_links:
    - from: "tests/test_overlay_customization.py"
      to: "app.OVERLAY_COLORS"
      via: "from app import OVERLAY_COLORS"
      pattern: "from app import.*OVERLAY_COLORS"
    - from: "tests/test_overlay_customization.py"
      to: "app.draw_date_overlay"
      via: "from app import draw_date_overlay with new kwargs"
      pattern: "draw_date_overlay\\(.*style="
---

<objective>
Wave 0 (TDD RED): Create the failing test contract file that locks every Phase 6 interface
BEFORE any production code is written. This file defines the OVERLAY_COLORS dict shape, the
extended draw_date_overlay() signature, and the rendering behavior of background vs outline
modes. Plans 02 and 03 turn these RED tests GREEN.

Purpose: Lock contracts up front so executors of Plans 02/03 implement against fixed
expectations, not assumptions. Prevents signature/behavior drift.
Output: tests/test_overlay_customization.py with 7 failing tests (TC-01..TC-07).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-text-customization-colors-styles-and-border-mode/06-CONTEXT.md
@.planning/phases/06-text-customization-colors-styles-and-border-mode/06-RESEARCH.md
@.planning/phases/06-text-customization-colors-styles-and-border-mode/06-VALIDATION.md

<interfaces>
<!-- Contracts under test. Executor writes tests asserting these; production code lands in Plans 02/03. -->

OVERLAY_COLORS dict (to be created in app.py near palette at app.py:196):
```python
OVERLAY_COLORS = {
    'black':  (0,   0,   0,   255),
    'white':  (255, 255, 255, 255),
    'yellow': (255, 216, 0,   255),
    'red':    (229, 57,  53,  255),
    'blue':   (0,   76,  255, 255),
    'green':  (29,  185, 84,  255),
}
```

Extended draw_date_overlay() signature (to be created in app.py:95):
```python
def draw_date_overlay(
    output_img, text, font, position_str, padding=6, rotation=0,
    style="background",
    bg_color=(0, 0, 0, 255),
    text_color=(255, 255, 255, 255),
    border_color=(255, 255, 255, 255),
    stroke_width=2,
):
```

Existing conftest fixtures (tests/conftest.py) — REUSE these, do not redefine:
- blank_rgb_image       -> small white RGB PIL.Image
- large_rgb_image       -> larger white RGB PIL.Image
- dejavu_or_default_font -> DejaVuSans-Bold 26 if available, else PIL default
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing contract tests for OVERLAY_COLORS and draw_date_overlay() modes</name>
  <files>tests/test_overlay_customization.py</files>
  <read_first>
    - tests/test_date_overlay.py (replicate the import-inside-test + fixture-arg style exactly)
    - tests/conftest.py (reuse blank_rgb_image, large_rgb_image, dejavu_or_default_font — do NOT redefine fixtures)
    - app.py:95-148 (current draw_date_overlay implementation — tests must match its viewer-canvas/paste behavior)
    - app.py:196-203 (palette RGB values that OVERLAY_COLORS must mirror)
  </read_first>
  <behavior>
    - TC-01 test_overlay_colors_dict: OVERLAY_COLORS has exactly 6 keys {black,white,yellow,red,blue,green}; each maps to the exact RGBA tuple from app.py:196-203 with alpha 255.
    - TC-03 test_background_mode_uses_bg_color: calling draw_date_overlay(..., style="background", bg_color=(229,57,53,255), text_color=(255,255,255,255)) paints at least one pixel equal to (229,57,53) into the image (the filled rect).
    - TC-04 test_outline_mode_no_rect: calling draw_date_overlay(..., style="outline", ...) on a white image leaves NO solid filled rectangle of bg_color — assert the count of pixels equal to bg_color=(0,0,0) is dramatically lower than the same call in background mode (outline draws only stroke+glyphs, no rect fill).
    - TC-05 test_outline_mode_border_color: style="outline", text_color=(255,255,255,255), border_color=(229,57,53,255), stroke_width=2 produces at least one pixel equal to (229,57,53) (the stroke).
    - TC-06 test_default_params_match_current: draw_date_overlay(img, text, font, 'bottomRight', padding=6) (NO new kwargs) produces the SAME pixel result as the legacy behavior — assert a black pixel (0,0,0) exists (filled rect) and a white pixel (255,255,255) exists (text), proving defaults preserve current visual.
    - TC-07 test_stroke_width_zero: style="outline" with stroke_width=0 draws no stroke pixels of border_color distinct from text — assert no pixel equals a border_color set different from text_color when stroke_width=0.
  </behavior>
  <action>
    Create tests/test_overlay_customization.py following the exact style of tests/test_date_overlay.py
    (module docstring stating these are RED until Plans 02/03 land; `from app import ...` INSIDE each
    test function; fixtures passed as function arguments).

    Write these 5 test functions in this file (the remaining TC-02, TC-08, TC-09 are added in Task 2):

    1. test_overlay_colors_dict():
       from app import OVERLAY_COLORS
       assert set(OVERLAY_COLORS.keys()) == {'black','white','yellow','red','blue','green'}
       assert OVERLAY_COLORS['black']  == (0,0,0,255)
       assert OVERLAY_COLORS['white']  == (255,255,255,255)
       assert OVERLAY_COLORS['yellow'] == (255,216,0,255)
       assert OVERLAY_COLORS['red']    == (229,57,53,255)
       assert OVERLAY_COLORS['blue']   == (0,76,255,255)
       assert OVERLAY_COLORS['green']  == (29,185,84,255)

    2. test_background_mode_uses_bg_color(blank_rgb_image, dejavu_or_default_font):
       from app import draw_date_overlay
       img = blank_rgb_image
       draw_date_overlay(img, '05.01.2022', dejavu_or_default_font, 'bottomRight', padding=6,
                         style='background', bg_color=(229,57,53,255), text_color=(255,255,255,255))
       pixels = list(img.getdata())
       assert (229,57,53) in pixels   # filled rect uses bg_color

    3. test_outline_mode_no_rect(blank_rgb_image, dejavu_or_default_font):
       from app import draw_date_overlay
       bg_img = blank_rgb_image.copy()
       draw_date_overlay(bg_img, '05.01.2022', dejavu_or_default_font, 'bottomRight', padding=6,
                         style='background', bg_color=(0,0,0,255), text_color=(255,255,255,255))
       bg_black = list(bg_img.getdata()).count((0,0,0))
       outline_img = blank_rgb_image.copy()
       draw_date_overlay(outline_img, '05.01.2022', dejavu_or_default_font, 'bottomRight', padding=6,
                         style='outline', text_color=(0,0,0,255), border_color=(0,0,0,255), stroke_width=2)
       outline_black = list(outline_img.getdata()).count((0,0,0))
       # Outline mode paints only stroke+glyph pixels, NOT a solid filled rect -> far fewer black pixels
       assert outline_black < bg_black

    4. test_outline_mode_border_color(blank_rgb_image, dejavu_or_default_font):
       from app import draw_date_overlay
       img = blank_rgb_image
       draw_date_overlay(img, '05.01.2022', dejavu_or_default_font, 'bottomRight', padding=6,
                         style='outline', text_color=(255,255,255,255), border_color=(229,57,53,255), stroke_width=2)
       assert (229,57,53) in list(img.getdata())   # stroke uses border_color

    5. test_default_params_match_current(blank_rgb_image, dejavu_or_default_font):
       from app import draw_date_overlay
       img = blank_rgb_image
       draw_date_overlay(img, '05.01.2022', dejavu_or_default_font, 'bottomRight', padding=6)
       pixels = list(img.getdata())
       assert (0,0,0) in pixels        # default bg rect is black
       assert (255,255,255) in pixels  # default text is white

    NOTE: anti-aliasing may produce near-but-not-exact colors. Use exact-tuple membership
    checks against getdata() as above (the rect fill and stroke_fill core pixels ARE exact;
    only glyph edges are anti-aliased). If a strict equality check proves flaky on the default
    bitmap font, fall back to asserting a count > 0 of pixels within +-2 per channel of the
    target color — but try exact first.

    Also add test_stroke_width_zero(blank_rgb_image, dejavu_or_default_font):
       from app import draw_date_overlay
       img = blank_rgb_image
       draw_date_overlay(img, '05.01.2022', dejavu_or_default_font, 'bottomRight', padding=6,
                         style='outline', text_color=(255,255,255,255), border_color=(229,57,53,255), stroke_width=0)
       # With stroke_width=0 there is no stroke, so the distinct border_color must NOT appear
       assert (229,57,53) not in list(img.getdata())

    Do NOT modify app.py in this plan. These tests MUST fail with ImportError/TypeError until
    Plan 02 lands — that is the intended RED state.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_overlay_customization.py -q 2>&1 | grep -Eq "(6 failed|error|ImportError|TypeError|cannot import)"</automated>
  </verify>
  <acceptance_criteria>
    - File tests/test_overlay_customization.py exists
    - `grep -c "def test_" tests/test_overlay_customization.py` returns 6
    - File contains `def test_overlay_colors_dict`
    - File contains `def test_background_mode_uses_bg_color`
    - File contains `def test_outline_mode_no_rect`
    - File contains `def test_outline_mode_border_color`
    - File contains `def test_default_params_match_current`
    - File contains `def test_stroke_width_zero`
    - File contains `from app import OVERLAY_COLORS`
    - File contains `style='outline'`
    - Running `.venv/bin/python -m pytest tests/test_overlay_customization.py -q` shows these tests FAILING (RED), not passing or erroring on collection of OTHER files
  </acceptance_criteria>
  <done>tests/test_overlay_customization.py exists with 6 failing tests (TC-01,03,04,05,06,07) that import OVERLAY_COLORS and call draw_date_overlay() with the new kwargs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add failing config-contract tests (TC-02, TC-08, TC-09)</name>
  <files>tests/test_overlay_customization.py</files>
  <read_first>
    - tests/test_overlay_customization.py (the file created in Task 1 — append to it)
    - app.py:34-52 (DEFAULT_CONFIG['immich'] — defaults the tests assert)
    - app.py:495-544 (update_app_config — global-read contract)
    - app.py:631-659 (settings() POST handler — form parsing / int cast contract)
  </read_first>
  <behavior>
    - TC-02 test_default_config_new_keys: DEFAULT_CONFIG['immich'] contains all 6 new keys with exact defaults: overlay_style='background', overlay_bg_color='black', overlay_text_color='white', overlay_border_color='white', overlay_stroke_width=2, overlay_font_size=26.
    - TC-08 test_update_config_new_keys: after update_app_config({'immich': {...new keys...}}), app.py module globals overlay_style/overlay_bg_color/overlay_text_color/overlay_border_color/overlay_stroke_width/overlay_font_size reflect the supplied values; missing keys fall back to defaults via .get().
    - TC-09 test_post_handler_font_size_int: the stored overlay_font_size and overlay_stroke_width globals are ints (not strings) after a config load — proves int() cast.
  </behavior>
  <action>
    Append three more test functions to tests/test_overlay_customization.py. These also remain
    RED until Plan 02 (DEFAULT_CONFIG keys + globals) and Plan 03 (POST handler) land.

    Add a section comment "# --- Config contract (TC-02, TC-08, TC-09) ---" then:

    6. test_default_config_new_keys():
       from app import DEFAULT_CONFIG
       imm = DEFAULT_CONFIG['immich']
       assert imm['overlay_style'] == 'background'
       assert imm['overlay_bg_color'] == 'black'
       assert imm['overlay_text_color'] == 'white'
       assert imm['overlay_border_color'] == 'white'
       assert imm['overlay_stroke_width'] == 2
       assert imm['overlay_font_size'] == 26

    7. test_update_config_new_keys():
       import app
       new_config = {'immich': dict(app.DEFAULT_CONFIG['immich'])}
       new_config['immich']['overlay_style'] = 'outline'
       new_config['immich']['overlay_bg_color'] = 'red'
       new_config['immich']['overlay_text_color'] = 'yellow'
       new_config['immich']['overlay_border_color'] = 'blue'
       new_config['immich']['overlay_stroke_width'] = 4
       new_config['immich']['overlay_font_size'] = 40
       app.update_app_config(new_config)
       assert app.overlay_style == 'outline'
       assert app.overlay_bg_color == 'red'
       assert app.overlay_text_color == 'yellow'
       assert app.overlay_border_color == 'blue'
       assert app.overlay_stroke_width == 4
       assert app.overlay_font_size == 40
       # backward-compat: a config missing the new keys must NOT raise (.get fallback)
       legacy = {'immich': {k: v for k, v in app.DEFAULT_CONFIG['immich'].items()
                            if not k.startswith('overlay_')}}
       app.update_app_config(legacy)
       assert app.overlay_style == 'background'   # fell back to default
       assert app.overlay_font_size == 26

    8. test_post_handler_font_size_int():
       import app
       new_config = {'immich': dict(app.DEFAULT_CONFIG['immich'])}
       new_config['immich']['overlay_font_size'] = 32
       new_config['immich']['overlay_stroke_width'] = 3
       app.update_app_config(new_config)
       assert isinstance(app.overlay_font_size, int)
       assert isinstance(app.overlay_stroke_width, int)

    Note for executor: update_app_config has import side effects (NTP/threads may be triggered
    elsewhere) but calling it directly with a dict is the established Phase 2 test approach — mirror
    how Phase 2 tests exercise config if any exist; otherwise direct call is fine since update_app_config
    only assigns globals and app.config. Do NOT modify app.py here.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_overlay_customization.py -q 2>&1 | grep -Eq "(9 failed|failed|error|AttributeError|KeyError)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" tests/test_overlay_customization.py` returns 9
    - File contains `def test_default_config_new_keys`
    - File contains `def test_update_config_new_keys`
    - File contains `def test_post_handler_font_size_int`
    - File contains `app.overlay_font_size`
    - File contains `isinstance(app.overlay_stroke_width, int)`
    - `.venv/bin/python -m pytest tests/test_overlay_customization.py -q` shows 9 failing tests (RED)
    - `.venv/bin/python -m pytest tests/test_date_overlay.py -q` still PASSES (no regression to Phase 2 tests)
  </acceptance_criteria>
  <done>All 9 TC-01..TC-09 contract tests exist and fail RED; the existing 13 Phase 2 tests still pass.</done>
</task>

</tasks>

<verification>
- `.venv/bin/python -m pytest tests/test_overlay_customization.py -q` => 9 failed (RED contract established)
- `.venv/bin/python -m pytest tests/test_date_overlay.py -q` => all pass (no Phase 2 regression)
- `grep -c "def test_" tests/test_overlay_customization.py` => 9
</verification>

<success_criteria>
tests/test_overlay_customization.py exists with 9 failing tests locking: OVERLAY_COLORS shape,
draw_date_overlay() extended signature, background vs outline rendering, default-preservation,
DEFAULT_CONFIG new keys, update_app_config global reads with .get() fallback, and int casting.
No production code modified. Phase 2 tests unaffected.
</success_criteria>

<output>
After completion, create `.planning/phases/06-text-customization-colors-styles-and-border-mode/06-01-SUMMARY.md`
</output>
