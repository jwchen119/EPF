---
phase: 06-text-customization-colors-styles-and-border-mode
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - app.py
autonomous: true
requirements: [TC-01, TC-02, TC-03, TC-04, TC-05, TC-06, TC-07]
must_haves:
  truths:
    - "OVERLAY_COLORS dict exists in app.py with 6 palette entries (TC-01 GREEN)"
    - "DEFAULT_CONFIG['immich'] has all 6 new overlay_* keys with backward-compat defaults (TC-02 GREEN)"
    - "draw_date_overlay() accepts style/bg_color/text_color/border_color/stroke_width; background mode unchanged, outline mode draws stroke with no rect (TC-03..TC-07 GREEN)"
    - "Default parameters reproduce the exact current visual (black rect, white text)"
  artifacts:
    - path: "app.py"
      provides: "OVERLAY_COLORS dict, extended draw_date_overlay(), new DEFAULT_CONFIG keys + module globals"
      contains: "OVERLAY_COLORS = {"
  key_links:
    - from: "app.py draw_date_overlay()"
      to: "PIL ImageDraw.text stroke API"
      via: "draw.text(..., stroke_width=, stroke_fill=) in outline branch"
      pattern: "stroke_width=stroke_width"
    - from: "app.py DEFAULT_CONFIG"
      to: "module globals"
      via: "overlay_* globals initialized from DEFAULT_CONFIG"
      pattern: "overlay_style = DEFAULT_CONFIG\\['immich'\\]"
---

<objective>
TDD GREEN for pure logic + config schema: add the OVERLAY_COLORS dict, extend
draw_date_overlay() with 5 new keyword args (style, bg_color, text_color, border_color,
stroke_width), add the 6 new DEFAULT_CONFIG['immich'] keys, and declare their module globals.
Turns TC-01..TC-07 GREEN. Does NOT wire UI or POST handling (that is Plan 03).

Purpose: Implement the rendering + schema contracts locked in Plan 01 with defaults that
preserve current visual behavior exactly (D-14).
Output: Modified app.py — OVERLAY_COLORS dict, extended draw_date_overlay(), new config keys + globals.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-text-customization-colors-styles-and-border-mode/06-CONTEXT.md
@.planning/phases/06-text-customization-colors-styles-and-border-mode/06-RESEARCH.md

<interfaces>
<!-- Current code to extend. Exact line anchors verified 2026-05-29. -->

Current draw_date_overlay() — app.py:95-148 (probe at 122-123, position at 128-129, draw at 132-136):
```python
def draw_date_overlay(output_img, text, font, position_str, padding=6, rotation=0):
    bw, bh = output_img.size
    if rotation in (90, 270):
        vw, vh = bh, bw
    else:
        vw, vh = bw, bh
    _probe = ImageDraw.Draw(Image.new('RGB', (1, 1)))
    bbox = _probe.textbbox((0, 0), text, font=font)
    tw = bbox[2] - bbox[0]
    th = bbox[3] - bbox[1]
    get_xy = POSITIONS.get(position_str, POSITIONS['bottomRight'])
    x, y = get_xy(vw, vh, tw, th, padding)
    viewer_canvas = Image.new('RGBA', (vw, vh), (0, 0, 0, 0))
    draw = ImageDraw.Draw(viewer_canvas)
    rect = [x - padding, y - padding, x + tw + padding, y + th + padding]
    draw.rectangle(rect, fill=(0, 0, 0, 255))
    draw.text((x - bbox[0], y - bbox[1]), text, fill=(255, 255, 255, 255), font=font)
    if rotation != 0:
        viewer_canvas = viewer_canvas.rotate(rotation, expand=True)
    overlay_rgb = viewer_canvas.convert('RGB')
    mask = viewer_canvas.split()[3]
    output_img.paste(overlay_rgb, mask=mask)
```

palette list — app.py:196-203 (authoritative RGB source for OVERLAY_COLORS).

DEFAULT_CONFIG['immich'] — app.py:34-52 (new keys appended after date_overlay_position at line 50).

Module globals init block — app.py:154-167 (new globals appended after date_overlay_position at line 167).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add OVERLAY_COLORS dict + 6 new DEFAULT_CONFIG keys + module globals</name>
  <files>app.py</files>
  <read_first>
    - app.py:34-52 (DEFAULT_CONFIG['immich'] — append new keys here)
    - app.py:154-167 (module global init block — append new globals here)
    - app.py:194-203 (palette list — OVERLAY_COLORS mirrors these exact tuples)
    - tests/test_overlay_customization.py (TC-01 test_overlay_colors_dict, TC-02 test_default_config_new_keys — implement to satisfy)
  </read_first>
  <action>
    1. In DEFAULT_CONFIG['immich'] (app.py:34-52), after the line
       `'date_overlay_position': 'bottomRight',  # D-05: default position` (line 50),
       add these 6 keys (D-14 defaults — preserve current visual exactly):
         'overlay_style': 'background',      # D-04/D-14: "background" | "outline"
         'overlay_bg_color': 'black',        # D-06/D-14: rect fill (background mode)
         'overlay_text_color': 'white',      # D-06/D-07/D-14: text glyph fill (both modes)
         'overlay_border_color': 'white',    # D-08/D-14: stroke color (outline mode)
         'overlay_stroke_width': 2,          # D-09/D-11/D-14: stroke px (outline mode), int
         'overlay_font_size': 26,            # D-12/D-13/D-14: font px, int

    2. Add the OVERLAY_COLORS dict immediately AFTER the palette list (after app.py:203).
       Use the EXACT RGBA tuples (alpha 255), mirroring palette at app.py:196-203:
         # Source: app.py:196-203 palette (authoritative). String name -> RGBA for overlay color resolution.
         OVERLAY_COLORS = {
             'black':  (0,   0,   0,   255),
             'white':  (255, 255, 255, 255),
             'yellow': (255, 216, 0,   255),
             'red':    (229, 57,  53,  255),
             'blue':   (0,   76,  255, 255),
             'green':  (29,  185, 84,  255),
         }

    3. In the module global init block (app.py:154-167), after the line
       `date_overlay_position = DEFAULT_CONFIG['immich']['date_overlay_position']` (line 167),
       add these 6 global initializers:
         overlay_style = DEFAULT_CONFIG['immich']['overlay_style']
         overlay_bg_color = DEFAULT_CONFIG['immich']['overlay_bg_color']
         overlay_text_color = DEFAULT_CONFIG['immich']['overlay_text_color']
         overlay_border_color = DEFAULT_CONFIG['immich']['overlay_border_color']
         overlay_stroke_width = DEFAULT_CONFIG['immich']['overlay_stroke_width']
         overlay_font_size = DEFAULT_CONFIG['immich']['overlay_font_size']

    Do NOT touch update_app_config(), the POST handler, or settings.html in this plan.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_overlay_customization.py::test_overlay_colors_dict tests/test_overlay_customization.py::test_default_config_new_keys -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "OVERLAY_COLORS = {" app.py` succeeds
    - `grep -q "'yellow': (255, 216, 0,   255)," app.py` OR `grep -Eq "'yellow':\s*\(255, ?216, ?0," app.py` succeeds
    - `grep -q "'overlay_style': 'background'," app.py` succeeds
    - `grep -q "'overlay_font_size': 26," app.py` succeeds
    - `grep -q "'overlay_stroke_width': 2," app.py` succeeds
    - `grep -q "^overlay_font_size = DEFAULT_CONFIG\['immich'\]\['overlay_font_size'\]" app.py` succeeds
    - `.venv/bin/python -m pytest tests/test_overlay_customization.py::test_overlay_colors_dict tests/test_overlay_customization.py::test_default_config_new_keys -q` exits 0
  </acceptance_criteria>
  <done>OVERLAY_COLORS dict, 6 DEFAULT_CONFIG keys, and 6 module globals exist; TC-01 and TC-02 are GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend draw_date_overlay() with style/colors/stroke params</name>
  <files>app.py</files>
  <read_first>
    - app.py:95-148 (current draw_date_overlay — the function being extended)
    - tests/test_overlay_customization.py (TC-03..TC-07 — background fill, outline no-rect, border color, default match, stroke_width=0)
    - .planning/phases/06-text-customization-colors-styles-and-border-mode/06-RESEARCH.md (lines 138-165: outline mode logic + textbbox stroke_width requirement)
  </read_first>
  <behavior>
    - Background mode: draws filled rect with bg_color then text with text_color (unchanged from current when defaults used).
    - Outline mode: NO rectangle; draws text with stroke_width + stroke_fill=border_color.
    - textbbox() probe passes stroke_width only in outline mode so padding accounts for stroke expansion.
    - Defaults (style="background", bg_color=(0,0,0,255), text_color=(255,255,255,255)) reproduce current visual exactly.
  </behavior>
  <action>
    Replace the signature and the draw block of draw_date_overlay() (app.py:95-148).

    1. New signature (app.py:95):
       def draw_date_overlay(
           output_img, text, font, position_str, padding=6, rotation=0,
           style="background",
           bg_color=(0, 0, 0, 255),
           text_color=(255, 255, 255, 255),
           border_color=(255, 255, 255, 255),
           stroke_width=2,
       ):

    2. Replace the textbbox probe (currently app.py:122-123):
       _probe = ImageDraw.Draw(Image.new('RGB', (1, 1)))
       sw = stroke_width if style == "outline" else 0
       bbox = _probe.textbbox((0, 0), text, font=font, stroke_width=sw)
       (Rationale per RESEARCH pitfall 1: stroke expands the bbox by stroke_width per side in
        outline mode; omitting it makes padding off by stroke_width px.)

    3. Replace the rect+text draw block (currently app.py:134-136):
       rect = [x - padding, y - padding, x + tw + padding, y + th + padding]
       if style == "background":
           draw.rectangle(rect, fill=bg_color)
           draw.text((x - bbox[0], y - bbox[1]), text, fill=text_color, font=font)
       else:  # outline — no rectangle, stroke provides separation (D-07)
           draw.text(
               (x - bbox[0], y - bbox[1]), text,
               fill=text_color, font=font,
               stroke_width=stroke_width, stroke_fill=border_color,
           )

    4. Leave Steps 4-5 (rotation + paste, app.py:138-148) unchanged.

    5. Update the function docstring first line to: "Draw a date overlay (filled-background or
       outline style) at position_str." Keep the existing rotation/padding arg docs; add a brief
       line documenting style/bg_color/text_color/border_color/stroke_width.

    Do NOT change the call site in scale_img_in_memory() — that is Plan 03. The defaults make the
    existing positional call (app.py:374) still produce identical output.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_overlay_customization.py -q -k "background_mode or outline_mode or default_params or stroke_width"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "stroke_width=2," app.py` succeeds (new signature default)
    - `grep -q "stroke_fill=border_color" app.py` succeeds
    - `grep -q "sw = stroke_width if style == \"outline\" else 0" app.py` succeeds
    - `grep -q "draw.text((x - bbox\[0\], y - bbox\[1\]), text, fill=text_color, font=font)" app.py` succeeds
    - draw_date_overlay signature line contains `style="background"`: `grep -q 'style="background"' app.py`
    - `.venv/bin/python -m pytest tests/test_overlay_customization.py -q -k "background_mode or outline_mode or default_params or stroke_width"` exits 0
    - `.venv/bin/python -m pytest tests/test_date_overlay.py -q` exits 0 (Phase 2 regression: defaults unchanged)
  </acceptance_criteria>
  <done>draw_date_overlay() supports background and outline styles with configurable colors and stroke; TC-03..TC-07 GREEN; Phase 2 overlay tests still pass.</done>
</task>

</tasks>

<verification>
- `.venv/bin/python -m pytest tests/test_overlay_customization.py -q -k "colors_dict or default_config or background_mode or outline_mode or default_params or stroke_width"` => 7 passed (TC-01..TC-07 GREEN)
- `.venv/bin/python -m pytest tests/test_date_overlay.py -q` => all pass (no Phase 2 regression)
- TC-08, TC-09 (config wiring) remain RED until Plan 03
</verification>

<success_criteria>
OVERLAY_COLORS dict, 6 DEFAULT_CONFIG keys, 6 module globals, and extended draw_date_overlay()
all exist. TC-01..TC-07 GREEN. Default parameters reproduce current visual exactly (Phase 2
tests still pass). UI/POST wiring untouched (TC-08/TC-09 still RED — Plan 03 closes them).
</success_criteria>

<output>
After completion, create `.planning/phases/06-text-customization-colors-styles-and-border-mode/06-02-SUMMARY.md`
</output>
